plugins {
	id 'java'
	id 'org.springframework.boot' version '3.5.14'
	id 'io.spring.dependency-management' version '1.1.7'
}

group = 'com.deepmesh'
version = '0.0.1-SNAPSHOT'

java {
	toolchain {
		languageVersion = JavaLanguageVersion.of(17)
	}
}

repositories {
	mavenCentral()
}

dependencies {
	implementation 'org.springframework.boot:spring-boot-starter-web'
	implementation 'org.springframework.boot:spring-boot-starter-data-jpa'
	implementation 'org.springframework.boot:spring-boot-starter-validation'
	implementation 'org.springframework.boot:spring-boot-starter-actuator'
	// 토폴로지의 노드·Pod 정보는 전부 K8s API가 출처다 (backend-frontend-api.md 1-2·1-3).
	// Spring Boot BOM이 관리하지 않는 의존성이라 버전을 직접 고정한다.
	implementation 'io.fabric8:kubernetes-client:6.13.5'
	compileOnly 'org.projectlombok:lombok'
	runtimeOnly 'com.mysql:mysql-connector-j'
	annotationProcessor 'org.projectlombok:lombok'
	testImplementation 'org.springframework.boot:spring-boot-starter-test'
	testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
	testCompileOnly 'org.projectlombok:lombok'
	testAnnotationProcessor 'org.projectlombok:lombok'
	// H2 — 수집 경로 테스트를 K8s·MySQL 없이 돌리기 위한 인메모리 DB
	testRuntimeOnly 'com.h2database:h2'
}

// 경로·소스에 한글이 있어 인코딩을 UTF-8로 고정한다 (미지정 시 Windows 기본
// 코드페이지로 컴파일돼 테스트 클래스 로딩이 깨진다).
tasks.withType(JavaCompile).configureEach {
	options.encoding = 'UTF-8'
}

tasks.named('test') {
	useJUnitPlatform()
	systemProperty 'file.encoding', 'UTF-8'
}
