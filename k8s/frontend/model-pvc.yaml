apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: frontend-model-pvc
  namespace: deepmesh
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
